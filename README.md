# Basic Chatting App via Console

## How to run?

You need to compile `client` and `server` apps first. Then run `cargo run` on server, then client.
You can run another client and see how simple it works.